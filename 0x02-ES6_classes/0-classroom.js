/**
 * implementing a class called classRoom
 * @param {maxStudentsSize} as an attribute of the class
 * @param {Number} assined to @param {maxStudentsSize}
 * @return {Number} from the class classRoom
 */

export default class classRoom {
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}
